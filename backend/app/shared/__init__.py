"""Shared module for common business logic.

This module provides shared schemas, utilities, and services that
are used across multiple features but contain business logic
(as opposed to infrastructure in core/).

Modules:
    schemas: Common Pydantic schemas for API responses.
"""
